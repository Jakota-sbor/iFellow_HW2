package org.example;

public class KIA extends Car {
    public KIA(String model, int year, String transmission, String color, int enginePower) {
        super(model, "Kia", year, transmission, color, enginePower);
    }
}
